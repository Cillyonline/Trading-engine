# Quickstart - Cilly Trading Engine

This document is a navigation shortcut, not a source of truth. Follow the
canonical documents below in order.

## 1. Set Up the Repository

Use `docs/getting-started/getting-started.md` for the canonical clone, virtual environment, and
dependency-install steps.

## 2. Start and Verify the Local API

Use `docs/getting-started/local-run.md` for the canonical startup command, `/health`
verification, API exercise flow, and reset procedure.

## 3. Run the Deterministic Smoke Run

Use `docs/testing/smoke-run.md` for the exact smoke-run command, expected output, and
exit-code contract.

## 4. Run the Test Suite

Use `docs/testing/index.md` for the canonical repository test command and links to
specialized determinism and snapshot docs.
