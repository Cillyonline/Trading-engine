services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/cilly_trading.db
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  sqlite_data:
